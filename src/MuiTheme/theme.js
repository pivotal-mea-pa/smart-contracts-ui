import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#006500'
    },
    secondary: {
      main: '#c6ab6e'
    },
    grey: {
      main: '#ccc'
    }
  }
});

export default theme;
